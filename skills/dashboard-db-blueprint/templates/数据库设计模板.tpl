# {系统名} 数据库设计（支撑所有页面查询）

> 通用方法论框架。业务实体（{实体名}）、字段、枚举由项目实际定义；本模板只规定"怎么设计才不重复、逻辑正确"。

## 一、设计原则
| # | 原则 | 落地方式 |
|---|------|---------|
| 1 | 一实一档一码 | 业务主键全站唯一；关系表双向引用主键 |
| 2 | 3NF | 事实表只存源头业务事实；受控冗余 ≤3 处且单向同步 |
| 3 | 事实表 + 视图分离 | 聚合指标一律 v_* 视图，不落库 |
| 4 | 统一维度字典 | 固定枚举全部进 sys_dict_item |
| 5 | 历史可溯 | 变化量用快照表（主-明细，任意维度集合），只追加 |
| 6 | 幂等同步 | 来源系统 T+1，自然键 upsert，审计进 data_sync_log |
| 7 | 行级数据权限 | sys_role.data_scope（all/org/self）查询层统一过滤 |

## 二、概念模型（ER 总览）
```
base_{org} ◄── base_{entity}（主数据）
                    ├─► {域}_fact*（业务事实，append-only）
                    ├─► {域}_relation（关系）─► {域}_plan / {域}_process / {域}_calendar
                    ├─► {域}_input（资源/投入）
                    ├─► {域}_member ── {域}_tier（画像分层）
                    └─► {域}_warn（风险）─► risk_task / risk_suggestion
```

## 三、事实/基础表（N 张）
（每表：表名 + 用途 + 字段表[字段|类型|约束|说明] + 索引；全部带 COMMENT ON TABLE/COLUMN 中文注释）

## 四、派生视图清单（v_*）
| 视图 | 计算来源 | 服务页面 |

## 五、约束与索引
- FK 全部引用主键表；部分唯一索引（同一时刻至多一条"生效中"）
- CHECK：score∈[0,100]、end≥start、amount≥0
- 事件/流水类表只追加（禁止 UPDATE/DELETE，审计要求）
- 高频查询索引：按 主键分组/日期范围/状态过滤 三类设计

## 六、同步与一致性
| 来源系统 | 目标表 | 同步方式 |
|---------|--------|---------|
| {来源系统A} | {表名} | T+1 全量比对 + 增量 upsert（自然键） |

一致性规则：
1. 所有同步按自然键幂等 upsert，重复推送不产生脏数据
2. 事件类只追加；主数据变更走"变更记录 + 受控冗余同步"双通道
3. 受控冗余字段由同步任务单向维护，业务更新不允许直接写
4. 时间口径统一以业务发生日期为准；报告周期由起止日期推导

## 七、与旧清单对照（去重说明）
| 旧清单 | 本设计处理 | 原因 |
|--------|-----------|------|
| {聚合表} | → v_* 视图 | 纯聚合勿存 |
| {枚举表} | → sys_dict_item | 固定枚举入字典 |
| {宽表} | → 主-明细 | 支持任意维度集合 |
